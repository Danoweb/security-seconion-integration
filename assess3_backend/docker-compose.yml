version: '3'
services:
  assess3_seconion_api:
    container_name: assess3_seconion_api
    build: 
      context: .
    network_mode: "host"
    privileged: true
    #ports:
    volumes: 
      - ./src:/usr/app/api
